# StoryHook 3.0.0 launch report

## Authorization and scope

Mikey requested that dev merge into main with a merge commit, then that 3.0.0 ship from main and install locally. All launch issues are fixed here without separate stories. SH-718 tracks the release. Mitigation agents use Astra at xhigh effort; council seats use Astra at medium effort.

## Plan

| Step | Status |
|---|---|
| Reconcile dev and main; preserve both branches' intended behavior | Complete |
| Repair gate evidence, commit the merge, and bump to 3.0.0 | In progress |
| Run the full versioned gate and land the main/dev PRs | Pending |
| Build four archives, verify uploads, and publish on GitHub | Pending |
| Install and verify the CLI, daemon, and plugins | Pending |

## Included prerequisites

| Story | PR | Confirmed merge |
|---|---|---|
| SH-653: verifier-owned cleanup | #761 | fa77273c15e1c43dedc07bcb2c44c2608e1b7f87 |
| SH-715: submenu hover | #815 | c09ee222fddfd29ef9f2fad4f2b31487e2a8ea99 |
| SH-716: provider activation | #817 | 8f5aa705bb8753c94305c0342d39e1ab28a45bfd |
| SH-717: story reset menu | #816 | e90bb320b9418d3c20f96dfdb3fc1fc032ff3dbb |

## Launch issues

| Issue | Evidence | Resolution and validation |
|---|---|---|
| Main and dev diverged | main 05a6f7816, dev 8f5aa705b; merge preview has 25 conflicting files | Reconcile main-only landing guards, reset, STE checks, process identity and display wake protection with newer dev behavior. Integration implemented; targeted coverage below. |
| Reset has two intentional contracts | Native reset preserves committed branches; the typed-ID card operation discards the named lane | Preserve both public contracts behind shared transactional ownership and workspace locking. Implemented; native and card regressions pass together, 27/27. |
| Migration numbers represent different histories | Main uses 38/39 for landing/reset; dev uses 38–43 for other changes and reset | Council chose a transactional lineage bridge to canonical schema 44, with verified backup and preservation of pending journals. Bridge and upgrade matrix pass, including pending journal preservation. |
| Notification identity and active-pane selection disagree | Main supports the verified agent in an inactive split; dev's resource reader selects the active pane | Council chose location-only resource discovery followed by exact process targeting and revalidation. Implemented; 25 identity cases, three notification fixtures and dispatch identity pass. |
| Duplicate reset dialogs and handlers after automatic merge | Seven duplicated DOM IDs and three duplicate handler definitions; two regression tests failed | Kept the typed-ID card flow; removed duplicate markup, handlers, bindings and unreachable legacy REST action. Both structural regressions now pass; browser validation pending. |
| Generated operational evidence fails new writing checks | Production STE checker rejected a block-delivery diagnostic: 29 words and a contraction | Quote literal diagnostics and continuation JSON at the producer. Checker probe now passes; end-to-end regressions added. Reset prose split into short sentences. |
| Test selector emits module paths as Cargo targets | Isolated production selector returned `story_reset/card` for a changed nested module | Require explicit impact owners for nested modules or run ALL. Deleted test targets cannot be emitted. Reproduction now returns ALL; regression tests added. |
| Verification API drift across branches | Compile checks found capture-failure fields, candidate fields, and legacy reset calls out of sync | Preserve structured capture error and output; align fixtures with final APIs. All 1,704 library tests pass; test imports and trait implementations are aligned. |
| Landing subprocess omitted isolated environment | New landing path did not carry child environment used by verifier mirror fixtures | Apply the existing child-variable and mirror environment boundary; regression expanded to landing attempt and recovery. |
| Migration shape and concurrent-upgrader gaps found in review | Matching column names did not prove constraints; an older upgrader could change lineage after inspection | Validate complete supported table definitions and reclassify under one transaction for migrations 38–44. Expanded lineage matrix passes 7/7; migration suite passes 65/65. |
| Native tmux inventory depends on locale | Under `LC_ALL=C`, tmux converted tab separators to underscores | Explicit UTF-8 tmux mode preserves the inventory protocol. Resource and identity regressions pass. Native reset caller-window fixture now requests the JSON it asserts. |
| Background comments invalidate native reset preflight | Block-delivery comments advanced the story head without changing reset authority | Permit only appended observations; retain rejection for lifecycle, awaiting and ownership changes. Library regressions pass, including allowed observations and rejected authority changes. |
| Verification outcome comments violate STE checks | Combined queue run rejected generated conflict, red and origin-mismatch text | Split unleased remediation prose at its shared producer. Four-case policy regression added; queue suite now passes 99/99. |
| A new hold can hide unresolved landing recovery | Exact regression accepted awaiting while a durable landing still owned the submission | Reject awaiting at the final landing-intent validator. Red confirmed; landing suite now passes 16/16. |
| Native reset can race project retargeting | Project slug, prefix and checkout could change between preflight and reservation | Revalidate project identity inside the reservation transaction. Regression added. |
| Typed reset receipt identity can disagree with its row | Deserialized project/story/token fields lacked a key match | Reject corrupt receipt identities before any cleanup; regression added. |
| A blocker added during PR submission hid an existing PR | Queue regression returned MissingPullRequest after remote publication completed | Preserve publication for the same generation and pinned identities while keeping execution held. |
| Merge omitted exhaustive command and verifier fixture updates | Full compile found native Reset missing from the blocking-path classification and missing landing methods/fields in verifier fixtures | Retain exhaustive classification and implement the new certification/landing contract in every fixture. All workspace targets subsequently passed clippy with warnings denied. |
| Rust battery omitted crate-local integration test addressing | Full gate rejected the ste-lint crate’s lint target before execution | Resolve each target through Cargo workspace metadata for both discovery and execution. Metadata-based resolver implemented for discovery and execution; recording regression passed red→green. All four battery-completion regressions passed; full battery still required. |
| Dispatch collision recovery reports a busy workspace | Plugin collision fixture expected recoverable artifacts but received an ownership refusal | Real tmux reproduction proved a newly started server inherited the dispatch lock. Guardian implemented and real tmux regressions pass for direct and inherited lock descriptors; it retains the lock during handoff while closing it at the detached-server boundary. Existing destructive-child inheritance stays required. |
| Multi-lane terminal fixture records the wrong pane | Engine dispatch accepted the first pane and correctly refused later panes whose stored identity remained `%1` | Record each returned pane ID at the fixture origin. Six-lane dispatch regression now passes. |
| Startup-death and isolation fixture drift | One fixture exited before launch identity capture; a lexical inventory rejected the identity test wrapper | Make the death event occur at its intended readiness boundary and use the normal isolation wrapper with explicit real-tmux selection. Sentinel, isolation and native identity regressions pass. |
| Overlapping Rust diagnostics and browser rebuild invalidated test evidence | Chair started a Rust battery while the full gate reached E2E; its ordinary Cargo build changed the artifact mode later Rust fixtures leased. Three fault-injection assertions failed. Three earlier CLI children also exited without status diagnostics. | Cancel both runs, preserve logs, run standard orphan cleanup, and serialize all phases for the next gate. No passing release receipt was minted. The sequential bare-ID suite passes 12/12 and fault-injection suite passes 4/4. Exact earlier signal attribution remains unproven; no speculative binary-lease rewrite was made. |
| Browser test daemon was replaced by the installed old CLI | Isolated daemon PID 3782/port 59332 was replaced by `/Users/mikey/.local/bin/story` PID 6700/port 59711; it rejected schema 44 as newer than supported 38 | Block delivery was the sole daemon control helper missing its own STORY_BIN pin. Pin the helper to its daemon executable and browser subprocesses to their lease. Poisoned-environment regression failed before the origin fix and passed afterward. Original daemon and replacement metadata preserved before cancellation. |
| PR-check generated evidence violates STE | Existing service regression rejected generated 26- and 21-word sentences | Split authored prose at its producer and strengthen the existing repeated-poll regression; service suite passes 18/18. |
| Browser workers emit a color warning storm | Playwright forces FORCE_COLOR while the parent exports NO_COLOR; Node warns for each worker | Normalize the runner environment while preserving the no-color request. Browser harness contracts pass 23/23. |

## Council decision

The Astra medium panel voted 3–0 for proposal B through Codex native orchestration. [Full decision and audit](3.0.0-council/DECISION.md). The third research proposal was excluded because chair recording exceeded its retry deadline after a native agent-limit retry. All three seats voted. No repository files changed during council review.

## Validation

| Check | Result |
|---|---|
| Required SH-718 obviation review | No candidates, including after quota reset |
| Formatting and whitespace | cargo fmt --all -- --check and git diff --check passed |
| Whole-workspace lint | Clippy passed with -D warnings after command/fixture merge corrections |
| First full gate | Shell suite finished with four named failures. Rust discovery failures were repaired. Browser run lost its isolated daemon and was canceled; no release certification. |
| Upgrade matrix | Expanded lineage matrix 7/7 and existing migration suite 65/65 passed |
| Engine reset | 10/10 passed |
| Combined integration run 6 | Library 1,704/1,704; landing 16/16; ownership 3/3; resource 14/14; grammar 44/44; schema 7/7 passed. Reset 26/27 and queue 80/99 exposed remaining failures; subsequent run 7 passes both suites. |
| Sequential focused run 7 | 203/203 passed: bare IDs 12, battery completion 4, block delivery 15, fault injection 4, leg reuse 10, gate locks 14, PR check 18, reset 27, verification queue 99 |
| Browser harness contracts | 23/23 passed, including helper CLI routing and color environment |
| Provider runtime | All six launch-repair groups passed: collision, six-lane budget, sentinel readiness, fixture isolation, identity 25/25 and real tmux locks 2/2. Earlier notification and dispatch identity groups passed. |
| Focused integration run 2 | Block delivery 15/15, continuation 27/27, dashboard structure 2/2 and initial migration matrix 5/5 passed. Resource metadata and impact-map corrections subsequently passed. Native reset issues are tracked above. |
| Dashboard duplicate-ID and handler regression | Red: both tests failed. Green: both passed with direct rustc test harness. |
| Golden snapshot merge | Conflicts only concern sequence numbers. Reviewed every changed field: diagnostic blockquotes and deterministic event-order sequence values only. Golden corpus passes 30/30. |


## Second gate findings and delayed-delivery decision

The second gate ran with one machine-wide owner and no concurrent builds or source edits. Formatting, workspace clippy, build, and all 98 plugin groups passed. Its core and contract failures are recorded below. The browser matrix completed: Chromium 518 passed/6 failed; WebKit 504 passed/6 failed/14 skipped; mobile Chromium 90 passed/1 failed; mobile WebKit 89 passed/1 failed/1 skipped; untrusted-origin Chromium 2 passed. Both desktop engines found the same six failures, and both mobile engines found the same additional failure. This diagnostic run does not certify the release.

| Finding | Reproduction | Correction and required proof |
|---|---|---|
| Positive CLI fixtures contain newly forbidden contractions | Story creation rejects `Users can't log in`; custom-state listing setup rejects `Won't fix this one` | Use full words in positive fixtures. Keep rejection coverage in the writing checker. |
| Verification retry retains the machine gate lock | Green and red retry workers fail the next gate acquisition | Reap the watchdog's foreground timer before watchdog exit. STOP/CONT regression fails before the fix and passes afterward for normal and signal shutdown. |
| Dependency hold delays unrelated verification | Withdrawal test cannot advance the next story within its budget | Return ordinary held work to the queue immediately. Preserve retry limits for durable infrastructure incidents; cover hold, unblock, retry and halt. |
| Receipt fixture invents cleanup ownership | Durable lease guard rejects the fixture's synthetic candidate | First prove the forged candidate is rejected. Persist the actual cleanup event, derive the candidate, then prove the receipt applies. |
| Native reset bypasses effective-block delivery derivation | Writer inventory finds two raw mutation transactions | Use the shared story transaction boundary. Couple this change with revocation below so reset cannot leave a late interrupt for its replacement. |
| Delayed notification can target a replacement session | Pending Interrupt stores no target; target capture happens after reset releases ownership | Council voted 3–0 for atomic pending revocation plus inherited workspace exclusion through effect quiescence. Apply across block episode retirement, native/card/engine reset, dispatch, recovery and adoption. Existing Attempting effects keep their acknowledgement; exact Resume identity remains mandatory. |
| Upgraded pending notifications have no provable episode | Older pending rows contain no session authority | Canonical migration 44 retains their audit rows but marks Pending effects Superseded with a reason. Preserve native reset, landing and typed reset journals; never replay Attempting effects. |
| Successful helper leader can leave active descendants | Quiescent capture regression prints success while a child survives | Opt-in group-quiescent capture retains the workspace owner until descendants stop, with the original deadline and honest timeout. |
| Identity resolution silently registers a discovered session | Pure resolver regression observes a registration write | Make discovery read-only. Explicit ordinary-notify adoption requires locked pending revocation and exact process revalidation; Interrupt and Resume require prior registration. |
| Interrupt process identity has incompatible tuple shapes | Captured two-field process tuple reaches a native-token reader and raises IndexError | Preserve the native process start token throughout capture and comparison. Also distinguish two lifetimes within the same coarse timestamp. |
| Python effect children can outlive workspace exclusion | Controller-death regression sees the child lose the inherited lock | Propagate inherited ownership to effect children; release only after external work stops. |
| Contract scanners misclassify restored paths and syntax | Full contract leg reports reader/writer inventory, nested dispatch, heredoc and listing-only errors | Record precise legitimate readers; scan all event append helpers and nested modules; exclude nested flag arms and quoted heredoc contents; recognize listing-only browser selection. Add semantic counterexamples so true violations remain rejected. |
| Reset orphan fixture has no child guard | Fixture isolation inventory names the raw spawn | Use the shared ChildGuard and explicit kill/reap. |
| Real interrupt fixture supplies an unregistered generic process | Exact identity guard refuses the fixture before interruption | Register the captured real provider identity and preserve unregistered and respawn refusal assertions. |
| Native reset is absent from command reference | Reference coverage finds the restored verb undocumented | Document native branch preservation, awaiting preservation, and renewed force consent distinctly from typed card reset. |
| Landing fixture bypasses daemon containment | Verifier fixture hygiene names its command construction | Apply the shared daemon and Git environment boundary to landing-phase children. |
| A project can transfer identity during an attempted notification | Review found no delivery owner in the final SQLite transaction fence | Pin UUID, slug, prefix, checkout, story creation identity and exact Attempting row; release only its successful terminal acknowledgement. Lock-gated recovery waits for inherited ownership and retries while unrelated work proceeds. |
| Reset helpers can report success before descendants stop | Real helper/Git regression returns success while a child holds a barrier | Require quiescent capture at owned workspace, engine reset and card reset helper boundaries. Keep reservations and diagnose Unsettled outcomes honestly. |
| Context menu contains both reset actions | Chromium receives ten actions instead of nine; keyboard traversal reaches the old Reset | Remove the obsolete action and retain typed Reset. Keep existing exact order and keyboard assertions; add a pure menu-model regression. |
| Re-dispatch test accepts the previous success toast | Following test is refused because prior dispatch still owns shared fake-tmux state | Wait for the new dispatch's in-flight state to end before asserting its result or ending the test. |
| Mobile and desktop rows rebuild unchanged controls when time metadata changes | Both mobile engines lose Actions node identity; trace shows only Updated timestamp changed | Reconcile fixed child slots so timestamp/detail changes preserve unrelated controls and focus. Existing assertions stay; deterministic timestamp-change regression added. |
| DOM removal scanner omits replaceChild | Pure scanner misses both drawer and list reconciliation bypasses | Route both replacements through insertBefore plus detach, preserving ordering. Expanded scanner reproduces both bypasses and passes 4/4 after repair. |
| Notice fixture creates unrelated writing advice | Failed dismissal snapshot shows only a passive-voice advice notice for the fixture title | Use active fixture prose so the test isolates the intended durable notice. Product notice behavior remains unchanged. |

The second council's [decision and audit](3.0.0-interrupt-council/DECISION.md) records the alternatives and unanimous vote. Its evidence packet is included alongside the decision. No council source edits occurred. A scratch-path alias mismatch during status inspection was corrected to the canonical path; no council state changed during the failed inspection.

Regression-first validation reproduced ten delivery/reset failures and five transaction identity failures before source changes. The two quiescence test modules initially lacked explicit nested paths; compile identified both and their declarations were corrected. Formatting and full runtime validation continue below.

The first post-fix focused Rust run recorded 2,065 outcomes and exposed four remaining failures. Full workspace clippy passed with warnings denied. The subsequent normal build and all 100 provider groups passed, including the two new ownership groups and real process-identity cases. Earlier browser failure artifacts were archived before rerunning that matrix.

| Follow-up finding | Evidence and correction |
|---|---|
| Git startup failure omits the checkout path | The unusable-checkout regression retained the correct terminal status but lost its path. Add argv and checkout context at the Git capture-error boundary; keep the existing assertion. |
| Identity-barrier test prevents its own worker from advancing | Its repeated lock probe retained the contender while sleeping. Replace it with a test-local write barrier around real SQLite, then assert workspace exclusion once and preserve every changed-identity rejection. |
| Activity fixture expects an inaccurate timeout claim | Align the assertion with the new honest cleanup-request message; keep all output, progress-renewal and timeout checks. |
| Refused reset briefly retains an auxiliary controller lock in an unrelated fork | A deterministic real-service regression fails before the fix while the unrelated child remains between fork and exec. Explicitly release only card/engine controller exclusion on return or unwind. Preserve inherited WorkspaceLock ownership, durable reservations, and final transaction fences; a live-effect regression verifies that retry and checkout transfer remain excluded until the effect exits. |

The final focused Rust rerun passed all 1,767 checks: library 1,716; delivery authority 7; card/native reset 32; engine reset 12. This includes the new refusal/unwind regression and the live-child workspace/reservation exclusion regression.

The focused browser rerun passed 116 checks: Chromium 55; WebKit 53 with two existing keyboard-setting skips; mobile Chromium 4; mobile WebKit 4. The untrusted-origin project selected no cases under this focused filter. Both mobile engines pass the unchanged-control identity/focus assertion with a real timestamp change. Both desktop engines pass the formerly failing dispatch, notice and menu cases. The full release gate will still run all configured browser projects on the bumped tree.

## Third gate follow-up

The complete merge gate ran to completion with frozen source and one machine owner. Formatting, clippy, build and all 100 provider groups passed. Core ran for 617 seconds and failed three targets; checkout contracts ran for 832 seconds and failed five targets. This was a diagnostic result, not a release receipt. The fixes below preserve the production ownership guards.

| Finding | Evidence and correction |
|---|---|
| Positive engine reset fixtures name a nonexistent repository | Hardening and two run-model cases fail workspace admission at `/repos/original`. Use real isolated Git repositories. Keep exact lease assertions and prove recovery locks the original repository after the current checkout changes. |
| Golden corpus waits for acknowledgements of superseded intents | The first wait poisons the shared corpus and causes 30 failures. Keep SH-9 and SH-6 in todo and require exact Superseded records with no acknowledgement. Keep active SH-3 and require the helper's exact Unreached response and comment. Supply a real Git checkout; review the removal of the two obsolete comments and dependent sequence changes before accepting snapshots. |
| Native reset fixture races its claim's asynchronous Resume acknowledgement | Retained activity proves the first reset is refused while the real worker still owns the workspace; acknowledgement commits four milliseconds later. Wait for that exact no-session Resume outcome and observe actual lock release before the two unchanged reset calls. Never retry reset or release another owner's descriptor in the test. |
| New executor regression module lacks two inventory declarations | Reader and process censuses identify the new module. Declare its selected-project checkout writes and its bounded, joined shell probes with explicit reasons. The scanners remain unchanged. |
| Manual Python test loader cannot resolve a bundled sibling | Continuation contract reproduces ModuleNotFoundError. Make its import path match direct script execution. Production imports remain unchanged; three transcript regressions pass and the sibling-loader sweep finds no other missing path. |
| List comparison serializes HTML despite the dashboard safety contract | The markup guard rejects outerHTML; an older wiring assertion still expects whole-row reconciliation. Compare detached deep DOM snapshots through isEqualNode, retained in a WeakMap by live slot. Keep the global safety guard, update coarse list wiring checks, and prove runtime roving-tabindex changes preserve title identity. The pure probe rejects both serialization and a naive live-node comparison. |

The focused follow-up passed 333 checks across eight Rust targets; the only remaining differences were eight golden snapshots. An independent full-content comparison approved exactly the removal of SH-6/#3 and SH-9/#1 comments/events and their resulting global-sequence shifts. SH-3/#2 remained byte-identical; all other values, states, counts, identities and ordering were unchanged. Those eight candidates were accepted by their reviewed SHA-256 digests. The ordinary golden runner then passed all 30 tests with snapshot updates disabled. The updated DOM regression then passed both cases in mobile Chromium and both in mobile WebKit, including production roving tabindex and preserved title identity.

## Fourth gate evidence and scoped reruns

Mikey asked to avoid unnecessary reruns and to retest only impacted behavior after repairs. The fourth run completed formatting, clippy, all 4,277 core checks (589 seconds), and all 1,637 repository contract checks (834 seconds), with no failures. Its owner then stopped the superseded pre-version gate before the remaining build/provider stages; the standard orphan postlude passed. This canceled run earned no aggregate gate receipt.

The reuse audit found two defects in evidence handling. They are fixed here with focused regressions, without new stories or a new receipt format.

| Finding | Reproduction and correction |
|---|---|
| Repeating a selective gate promotes its result to a full merge gate | The cached wrapper skips recreation of transient tier metadata; Makefile defaults the missing value to gate. Real Make/Git/receipt fixtures reproduce the promotion. Always resolve selection and provenance, cache only the selected execution, and refuse missing provenance. Repeated subsets remain selective without reexecuting checks; ALL shares the ordinary core cache. An exact helper impact entry preserves future narrowing. |
| Tree identity omits staged additions and restores staged deletions | HEAD-seeded private indexes report unchanged identities after new tracked content changes. Materialize the logical current index with NUL-delimited paths, then fold in worktree bytes. Real Git cases cover staged additions, retained deletions, split/alternate indexes, corrupt indexes, drift refusal and certification of the eventual committed tree. Source index/object contents remain unchanged. The identity script is now a production build dependency for result reuse; formatting and lint evidence remain independent. |

Both repairs and the version bump are coalesced before one final full gate. Commit the resolved two-parent merge first so semver reads the complete history, then create the separate version commit. The guarded main PR and disposable dev synchronization PR must both produce that certified tree. All archives are built only after returning to the actual main tip. This composes the existing versioning, gate, landing, build and publication primitives without weakening certification or changing semver configuration.

The central impacted run passed all 125 checks across build identity, linked-worktree identity, push receipts, gate reuse, portable receipts, selective gating, gate tiers, and fixture isolation. Both new support files have explicit impact owners. The repaired tracked-tree identity equals Git’s staged tree, including the integration additions.

The version preflight also identified five early release tags without changelog entries. Their v0.2.0–v0.5.0 entries were reconstructed from the tagged local histories and commit dates, preserving every existing entry and tag. The repaired preflight passes all six checks with no warnings.

This repository report records the integration checkpoint. Final publication and installation evidence accompanies the completed report attached to the release.


## Versioned gate: cleanup synchronization

The first committed 3.0.0 candidate passed formatting (2 seconds), clippy (28 seconds), all 4,277 core checks (662 seconds), all 1,645 contract checks (852 seconds), the normal build (28 seconds), and all 100 provider groups (713 seconds). Chromium completed 521 passing cases and three failures. All three failures occurred during cleanup after their product assertions passed.

| Finding | Exact evidence | Correction |
|---|---|---|
| Browser cleanup races active block delivery | The held-Enter blocked-move test sent exactly one POST, received 200, and observed the moved card. Its cleanup DELETE for AA-171 arrived about 55 milliseconds later while operation 1 was Attempting and correctly returned 500. Both delayed-destination prompt tests passed their assertions, then hit the same guard for DD-2/operation 2 and DD-3/operation 3. | Wait at the shared cleanup boundary for the exact isolated-store delivery records to become terminal before deleting their story. Preserve Council C's Attempting ownership; do not retry DELETE, weaken its guard, or infer completion from a missing row. |

The failure traces, screenshots, and error contexts are retained in the launch evidence. The owned gate was stopped with status 143 after this common cause was established; partial WebKit results remain diagnostic only. Its normal orphan postlude passed. It earned no aggregate gate receipt.

The repair affects shared browser fixtures. The existing content-based evidence retains formatting, clippy, core, build, and provider results. Checkout contracts scan the whole tracked tree, and browser certification covers the whole configured matrix, so those two stages need fresh results. Individual browser passes cannot be combined into a full receipt. Use the next canonical browser run as the green regression run rather than performing a separate focused browser run first.

The unpublished candidate branch `release/3.0.0-integration` and version commit `162661c6cb0479e962ef3802b969370e7be62202` are preserved unchanged. The final branch starts from the same pre-version history, commits the complete fixture repair and this report, and then regenerates 3.0.0 metadata. This retains the two-parent dev/main merge and includes the repair in the generated changelog. The final VERSION-changing commit must contain the same tree as the actual main tip used to build the archives; no existing tag or published branch is rewritten.

During the required context refresh, both StoryHook reads completed and reported zero obviation candidates. A subsequent display-only text filter stalled; only that owned filter was stopped. The saved context remained intact, and no tracker operation failed.

The shared fixture repair now includes 17 committed deterministic cases. The read-only snapshot binds all seven project/story identity fields in one transaction, retains every observed delivery ID, and accepts only named terminal outcomes. Seventeen scratch checks passed, and both unsafe mutants failed: accepting Attempting as terminal and treating a disappeared row as completion. All five named drawer cleanup helpers share the same barrier; production deletion assertions remain unchanged. Browser integration green remains the next canonical matrix.
