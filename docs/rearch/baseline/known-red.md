# storyhook known-red tests

Generated by `scripts/capture-baseline.sh` — do not hand-edit; re-run the
script and commit the diff.

Captured: host `Psamathe` (Apple M1 Max, 10 cores) · Darwin 26.5.2/arm64 · rustc 1.97.1 (8bab26f4f 2026-07-14) (Homebrew) · commit `4179f48` (clean) · 2026-07-28T15:49:31Z

Tests that exist, are committed, and do **not** run under `make test`.

Every one is a deliberate debt with a named criterion for un-ignoring it.
The count below is asserted against `<binary> --list --ignored` at capture
time, so an `#[ignore]` added without a reason — or added at all — cannot
slip into the tree unnoticed.

Ignored: **2** of 1171 Rust tests.

| test | binary | source | reason / un-ignore criterion |
|---|---|---|---|
| `a_story_created_in_one_checkout_is_visible_from_the_other` | `worktree_truth` | `tests/worktree_truth.rs:158` | SH-46: two checkouts are separate databases; goes green at the W4 flip |
| `two_worktrees_of_one_repo_mint_colliding_ids` | `worktree_truth` | `tests/worktree_truth.rs:118` | SH-46: two checkouts are separate databases; goes green at the W4 flip |

## Running them anyway

```sh
cargo test --workspace -- --ignored                        # every ignored test
cargo test --workspace --test worktree_truth -- --ignored  # the SH-46 pair
```

The `worktree_truth` pair is **expected to fail** until W4, the flip: two
checkouts of one repository are two separate databases today, so they mint
colliding ids and cannot see each other's stories. Its captured red output is
in `docs/rearch/STATE.md` (W0.3 step log), and removing those two `#[ignore]`
attributes is W4's exit criterion — not a cleanup, the criterion itself.
