#!/usr/bin/env bash
# Repository-specific long-lived branch roles. Generic StoryHook code derives
# each repository's default branch; only this repository assumes these names.

readonly STORYHOOK_INTEGRATION_BRANCH=dev
readonly STORYHOOK_STABLE_BRANCH=main
